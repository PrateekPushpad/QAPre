{
  "name": "infoqapreneur",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client": "ng serve --prefix /clientSide",
    "server": "cd backend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd ./clientSide && npm run client\" \"npm run server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "mongoose-auto-increment": "^5.0.1"
  }
}
